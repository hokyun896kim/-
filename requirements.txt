# 데이터
yfinance>=0.2.40        # 무료 미국주식 시세/재무 데이터 (로컬 실행 시 사용)
pandas>=2.0
numpy>=1.24

# 뉴스 감성 분석 (규칙 기반, 사전 내장 → 오프라인 동작)
vaderSentiment>=3.3.2

# 대시보드
streamlit>=1.30
plotly>=5.18

# 차트 판독 (Claude 비전 모델)
anthropic>=0.49

# 설정/유틸
PyYAML>=6.0

# 테스트 (개발용)
pytest>=7.4
